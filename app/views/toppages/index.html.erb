<% if logged_in? %>

   <%= link_to  'ユーザー詳細に戻る', user_path(current_user) %>
    
    
   
<% else %>

  <div class='center jumbotron'>
    <div class="text-center">
      <h1> Welcome to the MeetingManager</h1>
      <%= link_to 'Login!', login_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>
